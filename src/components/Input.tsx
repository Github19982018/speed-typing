import React from 'react'

export const Input = ({content, input}) => {
  const cont =content.split('');
  const inp = input.split('');
  const next = inp.length;
  
  return (
    <div>
        

    </div>
  )
}
